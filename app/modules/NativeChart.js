import React, {useRef} from 'react';
import {View, requireNativeComponent, Text, StyleSheet, useColorScheme} from 'react-native';

const Chart = requireNativeComponent('ChartControl');

export default Chart;
